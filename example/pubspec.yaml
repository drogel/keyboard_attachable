name: example
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.10.6'

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  keyboard_attachable:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
